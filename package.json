{
  "name": "unitstool-starter",
  "version": "1.0.0",
  "description": "UnitsTool static site generator + starter site (full)",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "npx http-server ./dist -p 8080"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "fs-extra": "^11.1.1",
    "slugify": "^1.6.6",
    "marked": "^5.1.1"
  }
}